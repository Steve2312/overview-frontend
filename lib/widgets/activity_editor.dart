import 'package:flutter/material.dart';

class ActivityEditor extends StatelessWidget {
  const ActivityEditor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
